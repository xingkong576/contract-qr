# 2026-05-27

## 浏览器自动化记录

- 尝试让 `openclaw browser` 和 `agent-browser` 操作用户已在 Edge 中登录的会话
- 现状：两个工具都启动独立浏览器实例，无法接入用户已有的 Edge 登录态
- 原因：浏览器之间隔离，Cookie/Session 不共享
- 解决方案待研究：
  - 方案 A：配置 `openclaw browser` 使用 `existing-session` 模式接入用户 Edge（需要调试 CDP 端口）
  - 方案 B：用户手动复制粘贴内容到已登录的豆包/头条号页面（当前最省事）
- 配置已加：`~/.openclaw/openclaw.json` 中添加了 `browser` 配置块，设 `defaultProfile: "user"` 和 `existing-session` 驱动
- 待继续：调试 `openclaw browser` 连接用户 Edge 的 CDP 端口，实现共用登录态
